= form_with model: [:admins, article], class: 'space-y-6' do |f|
  - if article.errors.any?
    .bg-red-50.border.border-red-200.rounded.p-4.mb-4
      %h2.text-red-800.font-semibold= "#{article.errors.count}件のエラーがあります"
      %ul.mt-2.list-disc.list-inside.text-red-700
        - article.errors.full_messages.each do |message|
          %li= message

  %div
    = f.label :title, 'タイトル', class: 'block font-semibold text-gray-700 mb-1'
    = f.text_field :title, class: 'w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'

  %div
    = f.label :body, '本文', class: 'block font-semibold text-gray-700 mb-1'
    = f.text_area :body, rows: 10, class: 'w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'

  %div
    = f.label :published_at, '公開日時', class: 'block font-semibold text-gray-700 mb-1'
    = f.datetime_field :published_at, class: 'border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'
    %p.text-sm.text-gray-500.mt-1 空欄の場合は下書きとして保存されます

  %div
    = f.submit class: 'px-6 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 cursor-pointer'
